<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kotlintemplates">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login.view.LoginActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".DiffUtilList.View.DiffUtilActivity" />
        <activity android:name=".SimpleLazyList.Views.SimplyLazyListActivity" />
        <activity android:name=".GridList.View.GridListActivity"></activity>
        <activity android:name=".RoomRetrofitList.View.RetroRoomActivity"></activity>
        <activity android:name=".KotlinTabs.Views.TabActivity"></activity>
    </application>

</manifest>